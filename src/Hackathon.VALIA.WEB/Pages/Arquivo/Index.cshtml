@page
@model Hackathon.VALIA.WEB.Pages.Arquivo.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Arquivo[0].NomeArquivo)
            </th>
            <th>
            <th>
                @Html.DisplayNameFor(model => model.Arquivo[0].User)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Arquivo[0].Status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Arquivo) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeArquivo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ArquivoId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ArquivoId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ArquivoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
